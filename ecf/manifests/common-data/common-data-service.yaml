apiVersion: v1
kind: Service
metadata:
  name: common-data-service
  namespace: ecf-namespace
spec:
  selector:
    app: common-data-service
  ports:
    - port: 9000
      targetPort: 9000
  type: ClusterIP